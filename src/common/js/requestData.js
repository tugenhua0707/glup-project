/*
 * 调用android和IOS的公用方法
 * @param params {object}
 * @param url {string} 请求的url
 * @param data {object} 请求的数据
 * @param callback {function} 需要调用的方法 该方法是全局的方法，需要在window下定义的；
 */
function requestData(params){
	// 自己的代码
}
module.exports = requestData;